# App/runtime
streamlit>=1.36,<2
pandas>=2.3,<3
numpy>=1.26,<2
requests
beautifulsoup4
python-dotenv

# LangChain family (keep these aligned)
langchain==0.2.12
langchain-community==0.2.12
langchain-openai==0.1.23
# DO NOT pin langchain-core; let the resolver pick (it’ll land on >=0.2.35)

# Vector store backing for your import path
chromadb>=1.3.2

# Tokenization / client
tiktoken
openai>=1.46
