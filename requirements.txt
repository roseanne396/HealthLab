# App/runtime
streamlit>=1.36,<2
pandas>=2.3,<3
numpy>=1.26,<2
requests
beautifulsoup4
python-dotenv

# LangChain family (keep these aligned)
langchain==0.2.13          # <-- bump from 0.2.12
langchain-community==0.2.12
langchain-openai==0.1.23
# Do NOT pin langchain-core; let resolver choose (it will pick >=0.2.35)

# Vector store for your import path
chromadb>=1.3.2

# Tokenization / client
tiktoken
openai>=1.46
