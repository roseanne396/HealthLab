# Core app
streamlit>=1.36,<2
pandas>=2.3,<3
numpy>=1.26,<2
requests
beautifulsoup4
python-dotenv

# LangChain family (keep these in lockstep)
langchain==0.2.12
langchain-community==0.2.12
langchain-openai==0.1.23
# Let langchain pull a compatible langchain-core (>=0.2.35). Don't pin core.

# Vector store
chromadb>=1.3.2

# Tokens / OpenAI client
tiktoken
openai>=1.46
